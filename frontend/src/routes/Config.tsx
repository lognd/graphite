// Config route skeleton (WO-G6 fills in the config editor with the
// regolith config where-doctrine source attribution).

import { EmptyState } from '../components/EmptyState/EmptyState';

export function Config() {
  return (
    <EmptyState
      title="Config editing lands with WO-G6"
      detail="Source attribution, reset-to-default, and real validation errors per the regolith config where-doctrine."
    />
  );
}
