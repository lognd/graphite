# Build artifacts
build/
dist/
*.egg-info/
# The committed fixture project's ship output is a deliberate exception
# (WO-G1: pytest coverage must pass without ../lithos, so the fixture's
# `dist/` is checked in, not regenerated on demand).
!tests/fixtures/**/dist/
!tests/fixtures/**/dist/**

# Node (dev-only frontend codegen tooling -- WO-G1; the real Vite app
# lands in WO-G2)
node_modules/

# Python
.venv/
__pycache__/
*.pyc
.pytest_cache/
.ruff_cache/
.mypy_cache/
.coverage
htmlcov/
coverage.xml

# frob local state
.frob/
FROBLEMS.md

# Secrets
.env
